import React from 'react';

const SignUp = (props) => {
    return (
        <React.Fragment>
            
        </React.Fragment>
    )
}

export default SignUp
///.then only first time after sign up, show the navbar s